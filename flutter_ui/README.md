# wing_ui

Flutter Windows desktop shell for wing.

The app embeds the existing Go Web UI with `webview_windows` and loads:

```text
http://127.0.0.1:10809/
```

Build from the repository root with:

```powershell
.\scripts\mk.ps1 ui
```
